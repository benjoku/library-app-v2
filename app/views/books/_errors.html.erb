<% if @book.errors.any? %>
  <h2>The following errors prevented the book from being saved: </h2>
  <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>